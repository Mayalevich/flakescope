"""flakescope — agentic CI flake categorization (PoC).

Pipeline: fetch failed CI runs -> extract a compact failure excerpt ->
categorize into a flake taxonomy (heuristic baseline or LLM) -> report.
"""
__version__ = "0.1.0"
